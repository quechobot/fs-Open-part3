{
  "name": "fs-open-part3",
  "version": "0.0.1",
  "description": "exercise-3.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "sudo rm -rf dist && cd /home/quechobot/fsOpen/fs-open-2024/part2/phonebook && npm run build && cp -r dist /home/quechobot/fsOpen/fs-Open-part3",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "quechobot",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "mongoose": "^8.2.0",
    "morgan": "^1.10.0"
  }
}
